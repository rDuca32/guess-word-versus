{
  "name": "guess-word-versus",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "dev": "concurrently -n WEB,SERVER,SHARED -c auto \"pnpm --filter shared dev\" \"pnpm --filter server start:dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter web build"
  }
}
