{
  "name": "guess-word-versus",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently -k -n SHARED,SERVER,WEB -c auto \"pnpm --filter shared dev\" \"pnpm --filter server start:dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter web build"
  }
}